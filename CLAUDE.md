# dongne-baljachwi — 동네 발자취

연인이 함께 다녀온 데이트 장소를 지하철 노선도 위에 기록하고, 사진과 일기로 추억을 남기는
**커플 전용** 데이트 아카이브 서비스.

**기술 스택 (2026-08-11 확정)**

| 항목 | 결정 |
|---|---|
| 프레임워크 | Vite + React + TypeScript (SPA, Next.js 아님) |
| DB / 인증 / 스토리지 | Supabase (Postgres + Auth + Storage), 클라이언트에서 직접 호출 |
| 배포 | Vercel |
| 커플 연결 | 초대 코드 방식 (소셜 로그인 아님) |
| 노선도 (메인 화면) | 직접 그린 SVG + 좌표 데이터 |
| 지도 보기 (보조 토글) | 카카오맵 |
| 역 좌표 데이터 | 전국도시철도역사정보표준데이터 (공공데이터포털 파일데이터, 2026-08-12 확정 — TAGO API는 실 호출 검증 실패로 폐기) |

수도권 지하철 기준. 모바일 우선 반응형, 데스크톱도 동일하게 지원.

> 기획 원본은 노션 "👣 동네 발자취 — 기획서"
> (https://www.notion.so/3b9df475418981b2ba1de496dfcc52b8) 이며, 그 내용을 그대로 옮긴
> `docs/PRD.md` 가 최종 소스다. 이 프로젝트는 `map`/`dongne-baljaguk` (동네발자국, 별개의
> 일반 대중 대상 서비스)와 무관한 독립 프로젝트다 — 서로 참고하지 않는다.
>
> 기존 클릭 가능한 React 프로토타입(목데이터, 로그인·백엔드 없음)이 있다 — 화면 구조와 인터랙션
> 참고용이며, 실 데이터/인증이 없는 상태이므로 구현 근거는 어디까지나 `docs/PRD.md`다.

## 협업 구조

작업은 6개 서브에이전트로 나뉜다 (`.claude/agents/`).

| 에이전트 | 역할 | 산출물 | 코드 수정 |
|---------|------|--------|----------|
| `planner` | 기획자 | `docs/` 기획 문서 | ✗ |
| `developer-frontend` | 개발자1 — UI/상태/렌더링/Supabase 클라이언트 호출 | `src/` 전체 | ✓ |
| `developer-backend` | 개발자2 — Supabase 스키마/RLS/쿼리, 역 좌표 적재 배치 | 스키마·SQL·배치 스크립트 | ✓ |
| `designer` | 디자이너 | 디자인 토큰, CSS, 비주얼 스펙 | 스타일만 |
| `qa` | QA (UI·통합) | 결함 리포트 | ✗ |
| `api-tester` | API 테스터 — 실 Supabase 프로젝트의 OpenAPI/REST/RPC를 직접 호출해 계약 검증 | 결함 리포트 | ✗ |

`api-tester`는 실 Supabase 프로젝트가 연결된 뒤(로컬 Postgres가 아니라 호스팅된 프로젝트)
API 계약 회귀 검증이 필요할 때 쓴다. `qa`가 화면·통합 흐름을 검증한다면, `api-tester`는
PostgREST가 실제로 노출하는 엔드포인트 자체(스키마·에러 봉투·RLS 응답)를 검증한다.

이 프로젝트는 순수 SPA다. `developer-backend`는 API 라우트를 만들지 않는다 — Supabase
스키마·RLS 정책·SQL 함수, TAGO API 연동 배치(역 마스터 데이터 적재) 스크립트가 담당 범위다.
런타임 요청은 `developer-frontend`가 Supabase JS 클라이언트로 직접 호출한다.

## 작업 흐름

```
요구사항 ──▶ planner ──▶ docs/specs/<기능>.md
                             │
                ┌────────────┴────────────┐
                ▼                         ▼
      developer-frontend ◀─계약─▶ developer-backend
                │
                ▼
            designer (토큰/스타일)
                │
                ▼
               qa ──▶ 결함 리포트 ──▶ 해당 개발자
```

- 스펙 없이 구현하지 않는다. 요구사항이 오면 먼저 `planner`를 돌린다.
- 프론트/백엔드는 스펙 4장(데이터 모델)을 공통 계약으로 삼는다. 계약을 혼자 바꾸지 않는다.
- `developer-frontend` 와 `developer-backend` 는 서로 파일이 겹치지 않으므로 병렬 실행한다.
- QA는 구현이 끝난 뒤 실행한다. 병렬로 돌리면 미완성 상태를 검사하게 된다.

## 공통 규약

- **주석은 한국어**로, '무엇'이 아니라 '왜'를 쓴다.
- 비즈니스 규칙이 코드에 박히는 지점에는 스펙 ID를 병기한다: `// F-03: ...`
- `any` 금지. 외부 데이터는 `unknown`으로 받아 경계에서 좁힌다.
- 색상·간격·타이포는 `src/styles/tokens.css` 의 CSS 변수만 사용한다. 리터럴 하드코딩 금지.
- Supabase `service_role` 키는 배치 스크립트(Node 등 서버 환경)에서만 쓴다. 브라우저 번들에
  절대 들어가면 안 된다 — RLS로 인가를 걸고, 클라이언트는 `anon` 키 + 로그인 세션만 쓴다.
- "나중에 쓸 것 같아서" 만드는 추상화·래퍼·옵션 파라미터 금지. 두 번째 사용처가 생기면 그때 추출한다.
- 검증(타입 체크·빌드·테스트)을 실제로 돌린 결과만 "통과"라고 보고한다.

## 문서

- `docs/PRD.md` — **노션 "👣 동네 발자취 — 기획서" 원문.** 모든 작업의 출발점
- `docs/specs/` — 기능별 상세 스펙 (아직 없음 — 필요할 때 `planner`가 이 PRD를 근거로 작성)
- `docs/decisions/` — 되돌리기 어려운 결정 기록 (ADR)
- `docs/design/` — 와이어프레임, 비주얼 스펙
