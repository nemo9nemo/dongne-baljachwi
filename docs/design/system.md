# 디자인 시스템 — 동네 발자취

구현: `src/styles/tokens.css` (단일 소스). 컴포넌트 CSS는 여기 시맨틱 토큰만 참조한다.

톤: 커플 전용 아카이브. **웜 로즈 + 샌드**를 기본으로 한다. 감성적이되 대비는 계산해서 지킨다.

---

## 1. 레이어 구조

```
원시 팔레트  --rose-* --peach-* --sand-* --ink-* --coral-* --amber-* --blue-*
      │  (이 이름을 컴포넌트에서 직접 쓰지 않는다 — 다크 전환이 깨진다)
      ▼
시맨틱 토큰  --color-* --app-* --shadow-* --space-* --radius-* --font-*
      │
      ▼
컴포넌트 CSS (*.module.css)
```

라이트가 `:root` 기본값, 다크는 `@media (prefers-color-scheme: dark)`에서 **시맨틱 토큰만**
덮어쓴다. 앱 내 테마 토글은 아직 없다.

---

## 2. 색 — 시맨틱 토큰

| 토큰 | 라이트 | 다크 | 쓰는 곳 | 쓰면 안 되는 곳 |
|---|---|---|---|---|
| `--color-bg` | `#fbf9fa` | `#171215` | 앱 바탕(=프레임 안쪽), body | 카드/입력 배경 (서피스가 사라진다) |
| `--color-surface` | `#ffffff` | `#1f181d` | 카드, 탭바, 다이얼로그, 입력의 hover/focus 바닥 | 넓은 화면 바탕 |
| `--color-surface-muted` | `#f4f1f2` | `#271f25` | disabled 컨트롤 바닥, 버튼 hover | 텍스트 위 오버레이 |
| `--color-field` | `#f6f2f3` | `#241d22` | 입력 필드 기본 바닥 | 버튼·카드 (입력이 아닌 것에 쓰면 신호가 흐려진다) |
| `--color-border` | `#e7e1e4` | `#33292f` | 카드 테두리, 구분선 | 입력 테두리(약해서 3:1 미달) |
| `--color-border-strong` | `#8e8489` | `#83737a` | 입력·버튼 테두리, 스크롤바 | 장식용 헤어라인(너무 진하다) |
| `--color-text` | `#1f181d` | `#f5eef2` | 본문·제목 | primary/danger 배경 위 |
| `--color-text-muted` | `#6d6469` | `#b6a5ae` | 보조 설명, 비활성 탭 라벨 | 12px 미만 텍스트, 에러 문구 |
| `--color-text-inverse` | `#ffffff` | `#1c1013` | primary/danger **배경 위** 글자 | 그 외 전부 (다크에서 값이 뒤집힌다) |
| `--color-primary` | `#bd4570` | `#f08bb0` | 링크, 기본 버튼 배경, 활성 탭 | 큰 면적 바탕 |
| `--color-primary-hover` | `#a3395f` | `#f6c9da` | 기본 버튼 hover | 기본 상태 |
| `--color-primary-soft` | `#fdf2f6` | `#3a1f2b` | 선택된 칩/셀 배경 | 텍스트 색 |
| `--color-danger` | `#c0392b` | `#f19087` | 에러 문구, 위험 버튼 테두리 | 일반 강조 |
| `--color-danger-soft` | `#fbeae8` | `#3a1c1a` | 에러 배너 배경 | 단독 사용(테두리 없이) |
| `--color-notice-bg` / `-border` | `#fff6e5` / `#f0d199` | `#3a2c15` / `#6b5320` | 안내 배너("상대 초대 대기 중") | 에러 |
| `--color-focus` | `#3b6fd4` | `#8ab4ff` | `:focus-visible` 아웃라인 전용 | 링크·강조색 (브랜드색과 혼동) |
| `--color-overlay` | `rgb(58 31 43 / 52%)` | `rgb(10 6 8 / 66%)` | 모달 딤 | 콘텐츠 위 텍스트 배경 |

### 검증된 대비 (계산값, WCAG 2.1)

| 조합 | 라이트 | 다크 | 기준 |
|---|---|---|---|
| `--color-text` / `--color-bg` | 15.8 | 16.2 | 4.5 |
| `--color-text-muted` / `--color-bg` | 5.4 | 7.9 | 4.5 |
| `--color-primary` / `--color-bg` (링크) | 4.7 | 8.0 | 4.5 |
| `--color-text-inverse` / `--color-primary` (버튼) | 4.9 | 8.0 | 4.5 |
| `--color-danger` / `--color-bg` | 5.2 | 8.0 | 4.5 |
| `--color-focus` / `--color-bg` | 4.5 | 8.9 | 3.0 |
| `--color-text` / `--color-field` (입력 글자) | 15.7 | 14.4 | 4.5 |
| `--color-border-strong` / `--color-surface` | 3.6 | 3.9 | 3.0 |
| `--color-border-strong` / `--color-field` | 3.3 | 3.7 | 3.0 |
| `--color-text-muted` / `--color-primary-soft` (disabled 버튼) | 5.2 | 6.4 | 4.5 |
| `--color-danger` / `--color-danger-soft` (배너 글자·링크) | 4.7 | 6.7 | 4.5 |

> `--color-primary`를 원래의 `#d3567f`(rose-500)에서 `#bd4570`(rose-600)으로 내렸다.
> rose-500은 흰 글자 대비가 3.9:1로 버튼 텍스트 기준을 못 넘겼다.
> rose-500은 장식(그라디언트·아이콘 채움)에만 남긴다.

> `--color-border-strong`를 `#d3cbcf`(sand-300)에서 `#8e8489`(sand-500)로 내렸다.
> sand-300은 흰 서피스 위 1.6:1로, 입력·버튼 경계로 쓰기엔 WCAG 1.4.11(3:1) 미달이었다.
> 다크의 `#4b3d45`(ink-500)도 1.7:1이라 같은 이유로 `#83737a`(ink-400)로 올렸다.

> **에러 배너 안의 링크는 `--color-danger`를 쓴다.** `--color-primary`는
> `--color-danger-soft` 바탕에서 4.2:1로 미달한다 (가입 화면 "이미 가입된 이메일" 배너).
> `.errorBanner a`가 이 규칙을 강제한다.

### 대비를 보장하지 못하는 구간

`--app-screen-wash`는 반투명 그라디언트라 그 위 색은 계산 결과가 달라진다.
워시 최상단(불투명도 10%) 기준:

| 그 위의 색 | 라이트 | 판정 |
|---|---|---|
| `--color-text` | 14.8 | OK |
| `--color-text-muted` | 4.9 | OK |
| `--color-primary` | 4.2 | **미달** |

→ **화면 상단 200px(워시 구간)에는 링크·primary 색 텍스트를 두지 않는다.**
워드마크 글자가 브랜드색이 아니라 `--color-text`인 이유가 이것이다.
워시 위에 올려야 하는 강조 요소는 자기 배경을 가진 것(`.errorBanner`, `.notice`)만 허용한다.

---

## 3. 앱 프레임 토큰

| 토큰 | 값 | 용도 |
|---|---|---|
| `--app-backdrop` | 웜 그라디언트 (다크: 근-검정) | ≥768px에서 프레임 바깥 배경 |
| `--app-frame-width` | `480px` | 프레임 고정 폭 |
| `--app-frame-max-height` | `900px` | 초대형 모니터에서 세로로 늘어지는 것 방지 |
| `--app-frame-radius` | `28px` | 프레임 모서리 |
| `--app-frame-border` | 흰 70% / 흰 10% | 프레임 외곽 1px |
| `--shadow-frame` | 2단 그림자 | 프레임 부양감 |
| `--app-screen-wash` | 로즈 10%→0% 그라디언트 (다크: 로즈 8%→0%) | `.screen` 상단 브랜드 밴드 |
| `--app-screen-wash-height` | `200px` | 워시가 사라지는 지점 = 헤더 블록 높이 |

`--app-screen-wash`는 **모바일 대책**이다. `<768px`에서는 프레임 백드롭이 아예 안 보여서
데스크톱만 브랜드 톤을 갖고 모바일이 더 밋밋해지는 역전이 있었다. 워시는 뷰포트가 아니라
`.screen`에 붙으므로 두 환경 모두에서 똑같이 보인다.

자세한 동작은 `docs/design/app-frame.md`.

---

## 4. 간격 · 반경 · 타이포 · 그림자

- 간격: `--space-1..12` = 4·8·12·16·20·24·32·40·48px. 4px 배수 외의 값 금지.
- 반경: `--radius-sm/md/lg/pill` = 6/**12**/16/999px.
  (md를 10→12로 올렸다. 컨트롤 높이가 48px이라 10px는 각져 보이고, 프레임 28px과도 계열이 안 맞았다.)
- 타이포 6단계: `xs 12 / sm 14 / md 16 / lg 20 / xl 26` + 초대 코드 전용 `code 40`.
  굵기 3단계(400/500/700), 행간 2단계(1.25 / 1.6).
  자간은 토큰이 아니다 — 워드마크(`0.18em`)·제목(`-0.02em`)·초대 코드(`0.08em`)처럼
  용도가 하나뿐인 값이라 해당 클래스에 직접 쓴다.
- 컨트롤 치수: `--control-height` 48px(입력·버튼 공통 최소 높이),
  `--touch-target` 44px(WCAG 2.5.8 하한 — 링크·탭 등 높이를 따로 잡는 것들의 바닥선).
- 그림자 4단계: `--shadow-card` / `--shadow-dialog` / `--shadow-frame` /
  `--shadow-button`·`--shadow-button-hover`.
  `--shadow-button`은 **primary 버튼 전용**이다. 미니멀 방향에서 색을 더 늘리는 대신
  높이로 주 동작을 구분하려고 둔 것이라, 아무 데나 쓰면 그 구분이 무의미해진다.
  라이트는 브랜드 톤(딥 로즈) 그림자, 다크는 검정 그림자가 안 보이므로 로즈 글로우다.

---

## 5. 접근성 규칙

- 포커스 링은 `index.css`의 `:focus-visible` 한 곳에서만 정의한다.
  컴포넌트에서 `outline: none`을 쓰지 않는다. `.input:focus`가 테두리를 브랜드색으로 바꾸는 건
  포커스 링을 **대체**하는 게 아니라 보태는 것이다 (링은 그대로 바깥에 뜬다).
- 터치 타깃 최소 44×44px. 입력·버튼 `min-height: 48px`(실측 51.6px),
  `.linkRow` 안의 텍스트 링크도 `min-height: 44px`(실측 44px), 탭바 각 탭 실측 55px.
- 색만으로 상태를 알리지 않는다.
  - 활성 탭 = 색 + 굵기 + 상단 인디케이터 바
  - 입력 에러 = 테두리색 + 바닥 틴트 + **문구**(`.error`)
  - 입력 비활성 = 색 + **파선 테두리**
  - 버튼 비활성 = 채움 제거 + muted 글자 (opacity로 흐리지 않는다 — 로즈 50% 위 흰 글자는
    대비가 무너진다)
- 링크는 밑줄을 유지한다(`index.css`의 `a`). 색만으로 링크임을 알리지 않는다.
- 이동·부양 효과(`transform`)는 `prefers-reduced-motion: reduce`에서 전부 끈다.
  색 변화만 남으므로 상태 구분은 유지된다.
- 지도/노선도 위에 얹히는 컨트롤은 반투명 금지. `--color-surface`(불투명)를 쓴다.
  `--app-screen-wash`는 이 규칙의 예외가 아니다 — 지도가 아니라 앱 바탕 위에만 깔리고,
  그 위 텍스트 대비를 §2에서 계산해 뒀다.

---

## 6. 브랜드

워드마크는 **텍스트 로고타입**이다 (`src/components/Wordmark.tsx`, `.brand`).
일러스트·캐릭터·장식 아이콘은 쓰지 않는다 — 미니멀 방향에서 위계는 타이포와 여백이 만든다.

- 글자: 서비스명 "동네 발자취", 14px / 700 / 자간 `0.18em` / `--color-text`.
  자간을 벌리는 것만으로 본문과 다른 층위로 읽힌다.
- 마크: 대각선으로 놓인 원 두 개 (발자국 추상화). `--color-primary`, 뒤 발자국은 `opacity: .5`.
  16×16 CSS 도형이라 에셋이 없다. 순수 장식이므로 `aria-hidden`.
- **미인증 화면에서만** 쓴다. 로그인 뒤에는 탭바가 정체성을 대신하므로 화면마다 반복하지 않는다.

---

## 7. 아직 정하지 않은 것 (placeholder)

`src/styles/tokens.css` 상단 주석과 동일하게 유지한다.

- **타이포 스케일** — 서비스 국문 서체 미선정. 서체가 정해지면 크기·행간 재조정.
  워드마크 자간(`0.18em`)도 서체에 따라 다시 잡아야 한다.
- **간격 스케일 정리** — `--space-10(40)` 사용처 확인 후 삭제 검토.
  (`--space-5(20)`는 `.form` 세로 간격·버튼 좌우 패딩으로 자리를 잡았다.)
- **노선 색상** — 실제 지하철 호선 색을 써야 하므로 `03-line-map` 스펙 확정 후.
- **감정·날씨 아이콘 / 방문·미방문 스탬프** — 정의 없음.
- **다크 모드 범위** — 프레임·배경·서피스·텍스트·상태색까지만 렌더 검증. 노선도 SVG,
  카카오맵 오버레이는 화면 자체가 없어 미검증.
